# Fireworks

An animation of colorful fireworks made with HTML Canvas.

## Usage

Run the commands below to build `dist/bundle.js`, then open `dist/index.html` to view the app.

```bash
npm install
npm run dev
```
